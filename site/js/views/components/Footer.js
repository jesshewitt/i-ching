
let Footer = {
    render: async () => {
        let view =  `
            <!-- The footer that can be named is not the true footer -->
        `

        return view
    }  
}

export default Footer;